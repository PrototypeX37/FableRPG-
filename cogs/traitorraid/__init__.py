"""Archived Traitor Raid extension.

The mode is intentionally disabled. Keeping this module as a no-op prevents a
stale deployment configuration from restoring its commands, while preserving
the implementation and historical feat records for reference.
"""


async def setup(bot):
    return None
